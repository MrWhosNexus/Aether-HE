# Python
__pycache__/
*.py[cod]
# Local virtualenv (rebuild from requirements)
venv-web/
venv/
# OS / editor
.DS_Store
*.swp
# PyInstaller build outputs (rebuild via build_installer.bat)
/build/
/dist/
*.spec.bak
# Vendored binary installers (re-downloaded by build_installer.bat)
vendor/*.exe
vendor/*.msi
